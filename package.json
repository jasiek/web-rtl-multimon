{
  "name": "web-rtl-multimon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "multimon-ng in the browser: tune an RTL-SDR over WebUSB, pick a channel off a live waterfall, FM-demodulate it, and decode POCSAG/FLEX/AFSK pagers with multimon-ng compiled to WebAssembly.",
  "scripts": {
    "build:wasm": "bash wasm/build.sh",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "rtlsdrjs": "file:vendor/rtlsdrjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
